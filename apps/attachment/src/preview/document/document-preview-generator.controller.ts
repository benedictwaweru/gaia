import { Controller } from '@nestjs/common';

@Controller('document-preview-generator')
export class DocumentPreviewGeneratorController {
  // This controller can be used to handle requests related to document preview generation
  // Add methods here to handle specific routes and logic for generating document previews
}
